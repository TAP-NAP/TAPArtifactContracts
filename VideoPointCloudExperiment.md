# 视频 RGB 点云空间留存实验

本实验探索一种服务于观看体验的 TAP Video 3D 播放方式：把视频中已观察到的区域
留在同一局部空间，让用户暂停后转动视角，回看刚才经过的场景。
RGB-D 指同一时刻的彩色图像与深度数据；空间图指由成功对齐的 RGB-D 观察累积的点云。
它用于视觉浏览，不承诺测量精度、完整重建或生产级性能。

## 播放规则

当前可用的 RGB-D 直接生成当前点云，不等待相邻帧，也没有时域平滑算法或开关。
当前投影与空间配准分别处理：前者展示当前画面，后者估计相机移动并更新空间图。
配准缓慢或失败时，当前画面仍更新；已接受的空间图保留，不接入未定位的新点。
只有成功对齐的新观察才能替换空间图中当前录制相机视野内的旧点。

离开录制相机视野的区域冻结其已投影点的位置、密度和 RGB；回到该区域时，用新观察更新。
历史和当前点使用相同的原始颜色、亮度与清晰点样式，不额外变暗、模糊或按时间稀释。
默认视角以当前画面为中心；手势只改变观看角度和缩放，不重写点云。
照片与视频 3D 均从 1 倍开始，可缩放至 0.1–3.2 倍，即最小为初始大小的 10%。

暂停保留整个已显示场景，用户可以手势浏览；继续播放保留累计区域。
拖动播放位置、离开 3D、切换素材或显式资源重置才清理空间图。
缺深度期间保留点的位置、RGB 和样本时间戳；如有可用的已录 Core Motion，
显示旋转跟随播放时间继续，平移维持最后可靠估计。没有可用运动数据时，旋转也保持。
Core Motion 不提供完整的六自由度相机位姿（3 个平移方向与 3 个旋转方向），
也不证明缺帧期间的几何位置正确。

## 成功判据

1. 镜头从一个区域移动到另一区域后，原区域仍可回看，保留原有点密度与 RGB；
   成功对齐的回访能更新该区域，固定镜头下变化的物体不持续堆叠旧副本。
2. 暂停后累计场景保持稳定，旋转和缩放可查看历史；继续播放不清空历史，
   真正的跳转或素材切换不会留下错误上下文。
3. 有可用 RGB-D 时，配准失败或耗时不让当前画面永久卡住；缺深度时不闪空白或无数据提示，
   已录运动只按上述边界推进显示旋转。
4. 当前与历史均无额外降亮、模糊或时域平滑；首次拖动不突变缩放，照片与视频都能缩至 10%，
   新帧不重置用户的观看变换。
5. 播放和手势不改写原始媒体、深度、校准、时间戳或签名证据；签名验证仍按原有契约运行，
   不把显示结果当作几何正确性的证明。

## 真机验收

1. 在同一台支持设备上，分别新录普通双摄和 PRO LiDAR 视频。覆盖固定镜头、移动物体、
   缓慢旋转、平移及移出后返回的场景；记录设备、相机模式与录制设置。
2. 从片头进入 3D，镜头移开后暂停，转动视角查看早先区域；继续播放至回访位置，
   比较区域更新、颜色、密度和动态物体残影。
3. 分别在照片和视频 3D 中测试首次拖动、捏合到 10% 和放大到 3.2 倍；
   检查暂停、继续及新帧到来时的视角与缩放保持，确认设置中没有平滑开关。
4. 用存在真实深度缺口和重叠不足的视频检查缺帧与配准失败；对照播放时间确认当前帧、
   保留几何、旋转和平移各自符合边界。再测试跳转、离开 3D、换素材和资源重置。
5. 按 [TAP Video 验收](Acceptance.md#video) 比较原件哈希、签名验证和导出读回。
   随新区域增加观察内存、帧延迟和温度，记录实际表现及失败场景。

目前已通过原生通用 iOS 设备目标编译、5 项 Mac 逻辑回归，以及原型 82 项 Node 测试；
原型浏览器缩小交互和控制台检查也已通过。这些检查不代替原生真机验收。
最近的 10% 缩放与完整删除时域平滑仍待真机确认。

## 技术范围与限制

实验沿用现有 RGB-D、校准与已录运动数据，不引入 ARKit、新依赖或新采集格式。
不做 Gaussian Splatting、遮挡或孔洞补全，也不投入旧素材兼容工程；
未被观察到的表面不能由留存点云补出来。

平移和局部空间对齐依赖 RGB-D 的有效深度、图像对应及足够重叠，
低纹理、快速运动或动态主体可能导致失败和漂移。
为保留冻结区域的原密度，累计的新区域越多，内存需求越大；当前没有长时播放性能承诺。
完整产品约束见 [ProductContract](ProductContract.md)，签名与媒体格式见 [契约索引](CONTRACTS.md)。
